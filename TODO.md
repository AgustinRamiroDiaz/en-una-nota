# Allow setting the playlist from the app

Need to check if possible with Web Playback SDK.

# Add visual cue of song being played

It needs to blur the info instead of hiding it

We can use a counter to show the number of songs played

# Add an ID for the app to show in spotify connect names

# Autoconnect as player in spotify connect
